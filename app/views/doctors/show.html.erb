<%= @doctor.name %>

    <ul>
    <% @doctor.appointments.each do |app| %>
        <li><%= link_to app.patient.name, patient_path(app.patient) %> <%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
    <% end %>
    </ul>